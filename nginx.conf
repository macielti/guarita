events {
    worker_connections 1024;
}

http {
    upstream api {
        server api1:8000;
        server api2:8000;
    }

    server {
        listen 9999;

        location / {
            proxy_read_timeout 5s;
            proxy_connect_timeout 1s;
            proxy_pass http://api;
        }
    }
}