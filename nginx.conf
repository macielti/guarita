worker_processes 1;

events {
    worker_connections 512;
}

http {
    upstream api {
        server api1:8000;
        server api2:8000;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://api;
        }
    }
}