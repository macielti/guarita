services:
  api:
    image: ghcr.io/macielti/guarita:latest
    ports:
      - "9999:8000"
    volumes:
      - ./resources:/app/resources
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: "350MB"